export const COLDSTART_MODEL_CALL_CAP = 12;

// Every recording gets a summary; this bounds how many are narrated in one tick so a
// backlog is worked through over several ticks rather than in one unbounded run.
export const RECORDINGS_NARRATED_PER_TICK = 25;

export const ORG_MODEL_CALL_CAP = 120;
