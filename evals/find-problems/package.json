{
  "name": "@growthmind/eval-find-problems",
  "version": "0.0.0",
  "private": true,
  "description": "Does the model find the problems a growth engineer would, from real recorded sessions? Drives our own app with model-driven personas, runs the result through the production replay path, and scores what comes back against a held-out answer key.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/amazon-bedrock": "^5.0.47",
    "@growthmind/adapters": "workspace:*",
    "@growthmind/core": "workspace:*",
    "@growthmind/shared": "workspace:*",
    "ai": "^7.0.42",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@rrweb/record": "2.1.1",
    "@types/bun": "^1.3.14",
    "playwright-core": "^1.62.1"
  }
}
