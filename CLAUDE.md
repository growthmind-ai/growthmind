# Growthmind — Contributor Guide

The guide lives in [AGENTS.md](AGENTS.md) — the stack, the commands, the
conventions, and the product decisions a PR is judged against. Read it before
changing anything here.

This file exists because Claude Code reads `CLAUDE.md`, and it does nothing but
send you there. Nothing is duplicated into it — two copies of the same rules
drift, and then nobody knows which one is the contract.
