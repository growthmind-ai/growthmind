// One home for the sentence, because it is now said in two places: the group layout, and
// the agent page's example half, which renders outside that group so a founder can reach
// the real half without being on the allow list.
export const PREVIEW_DISCLAIMER =
  "Example content. Nothing on this page was measured from a real product.";
